<div class= "container">
  <h1>Categorias</h1>
  <ul>
  <% @categories.each do |category| %>
    <li><%= link_to category.nombre, category_path(category) %></li>
  <% end %>
  |<%= link_to  "Nueva categoria...", 
    new_category_path %>|
  </ul>
</div>
