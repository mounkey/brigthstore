<%= simple_form_for(@category), :html => {class: "form"} do |f| %>
<%= f.input :nombre %>
<%= f.submit "Agregar Categoria", class: "btn btn-primary" %>
<%= link_to  "Volver a categorias", categories_path %>
<% end %>